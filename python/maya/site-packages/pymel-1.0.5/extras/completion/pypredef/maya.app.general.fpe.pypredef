import maya.OpenMaya as OpenMaya
import maya.cmds as cmds
import maya
import os

class FilePathEditor:
    """
    Class to get file path info, to populate the File Path Editor.
    """
    
    
    
    def __init__(self, windowName='filePathEditor'):
        """
        Initialize all variables to default values.
        """
    
        pass
    
    
    def append_filename_to_lists(self, full_filename, attr, f, uri):
        pass
    
    
    def close(self, *args):
        pass
    
    
    def create(self):
        """
        Create the File Path Editor window
        """
    
        pass
    
    
    def fill_filename_DB(self):
        """
        Fill the filename database, to be able to query it later.
        """
    
        pass
    
    
    def get_all_filenames(self):
        """
        Get all values for attributes used as filenames.
        Also get all filenames for references.
        Skip empty filenames (if ShowEmptyRows is empty).
        Ex. MyFile.jpg, OtherFile.jpg, SomeFile.tiff, MyRef.ma.
        """
    
        pass
    
    
    def get_num_rows(self):
        """
        Get the number of rows, i.e. the number of (non-empty) filenames.
        """
    
        pass
    
    
    def get_value_for_UI(self, row, column):
        """
        Function that is called when writing contents of cell (row, column).
        NB: in the scriptTable, the indexes are starting at 1, but they were
        converted to 0-basis (in print_cell_info) before calling the current
        function.
        
        For now,
        col 0 = Name,
        col 1 = Attribute,
        col 2 = URI,
        col 3 = Path,
        col 4 = Status.
        This will change when we add the possibility to add, remove or move
        columns.
        """
    
        pass
    
    
    def print_cell_info(self, row, column):
        """
        Return File Path Editor cell info for row and column.
        """
    
        pass
    
    
    def refresh(self, *args):
        """
        Refresh the window
        """
    
        pass
    
    
    def reset(self):
        """
        Initialize all variables to default values.
        """
    
        pass

def doIt(windowName='filePathEditor'):
    """
    Main function to create a FilePathEditor object and run it.
    """

    pass

