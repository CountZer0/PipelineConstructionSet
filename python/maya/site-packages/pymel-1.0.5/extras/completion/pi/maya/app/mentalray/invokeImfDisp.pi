import maya.cmds as cmds
import maya
import maya.mel as mel
import os
import subprocess
import sys

def invokeImfDisp(fileName, channel):
    """
    Display given image file using imf_disp that is packaged with Maya.
    
    If the image is an OpenEXR and a channel is defined then that channel
    will be displayed, otherwise the default channels (R,G,B,A) will be used.
    """

    pass

